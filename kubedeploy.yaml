
apiVersion: apps/v1
kind: Deployment
metadata:
  name: praveen-deploy
  labels:
    app: praveen-deploy-lbl
spec:
  replicas: 3
  selector:
    matchLabels:
      app: praveen-app
  template:
    metadata:
      labels:
        app: praveen-app
    spec:
      containers:
      - name: praveen-container
        image: praveenkumar112001/praveendocker:v2.0
        ports:
        - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: praveen-np-service
  labels:
    app: praveen-app
spec:
  selector:
    app: praveen-deploy-lbl

  type: NodePort
  ports:
  - nodePort: 31028
    port: 8080
    targetPort: 8080
